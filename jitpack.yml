jdk:
  - openjdk17
env:
  - GRADLE_VERSION=8.1.0
before_install:
  - sdk install gradle $GRADLE_VERSION
  - sdk use gradle $GRADLE_VERSION
build:
  - gradle :library:assembleRelease :library:publishToMavenLocal
